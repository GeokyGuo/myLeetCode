报数序列的关键思想，由当前的string串，得到下一个string串，于是就要把 轮数，以及getNextStr  分开来算，这样就不容易乱‘

统计一个字符有几个有两种思路，第一种是 把第一个元素拿出来，当成一个临时比较对象，字符串中的字符去遍历跟临时对象比较  while + if else 组合，count是每一轮加1
                            第二种是把 两两比较，两两比较这种， 可以用while + while ，内部的while可以刷count 内部的while其实和if else的逻辑很像，while后边的其实就是不满足if的else的域
                            两者区别是 边界值的区别，第二种如果用遍历而不是两两比较，边界需要单独处理。

【122题】 125